import backgroundMale from "@/assets/images/background/background_male.jpg";
import backgroundSymbol from "@/assets/images/background/background_symbol.png";
import darkmode from "@/assets/images/theme/darkmode.png";
import lightmode from "@/assets/images/theme/lightmode.png";
import logo from "@/assets/logos/logo.png"
import man from "@/assets/images/man.png";
import sportImage from "@/assets/images/sportimage.png";
import treadmill from "@/assets/images/treadmill.png";
import workout from "@/assets/images/workout.png";

/**
 * An object containing background image assets used in the application.
 *
 * @property {string} backgroundMale - Path to the background image for male users.
 * @property {string} backgroundSymbol - Path to the background image featuring a symbolic graphic.
 */
export const BackgroundImages = {
    backgroundMale,
    backgroundSymbol,
}

/**
 * An object containing theme-related image assets used in the application.
 *
 * @property {string} darkmode - Path to the image representing dark mode.
 * @property {string} lightmode - Path to the image representing light mode.
 */
export const ThemeImages = {
    darkmode,
    lightmode,
}

/**
 * An object containing miscellaneous image assets used in the application.
 *
 * @property {string} man - Path to the image of a man.
 * @property {string} sportImage - Path to the image related to sports.
 * @property {string} treadmill - Path to the image of a treadmill.
 * @property {string} workout - Path to the image related to workouts.
 */
export const MiscImages = {
    man,
    sportImage,
    treadmill,
    workout,
}

/**
 * An object containing the logo image asset used in the application.
 *
 * @property {string} logo - Path to the application's logo image.
 */
export const Logo = {
    logo,
}